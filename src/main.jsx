import React from 'react';
import ReactDOM from 'react-dom/client';
import './main.css'; // or './index.css' if that's your main CSS
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);